{
  "name": "client",
  "version": "1.0.0",
  "main": "index.mjs",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "node index.mjs",
    "build": "./esbuild.config.mjs",
    "buildandrun": "yarn build && node dist/index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@fastify/swagger": "^8.4.0",
    "@fastify/swagger-ui": "^1.8.1",
    "fastify": "^4.17.0"
  },
  "devDependencies": {
    "esbuild": "^0.17.19"
  }
}
